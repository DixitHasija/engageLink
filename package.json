{
  "name": "converter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:copy": "cp ./service-worker/firebase-messaging-sw.js ./dist/",
    "format": "npx prettier . --write"
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "firebase": "^11.0.1"
  }
}
